import { useContext } from "react";
import { ColorModeContext } from "../src/components/Menu/components/ColorMode";

export default function Video() {
    const context = useContext(ColorModeContext);

    // To do a video page in the future
}